#!/usr/bin/env ansible-playbook
---

- name: MEDIAVAULT
  hosts: mediavault
  tags: mediavault
  tasks:
    - name: Add backup
      ansible.builtin.include_tasks: ressources/add_backup_task.yml
      loop: "{{ vault_backups }}"

...
