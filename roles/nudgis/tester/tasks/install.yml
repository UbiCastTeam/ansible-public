---

- name: "Install tester packages"
  ansible.builtin.import_role:
    name: "nudgis/shared"
    tasks_from: "apt_install.yml"
  vars:
    apt_packages: "{{ tester_packages }}"
    apt_packages_install_recommends: true

...
