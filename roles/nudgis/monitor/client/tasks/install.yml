---

- name: "Install required packages for munin-node"
  ansible.builtin.import_role:
    name: "nudgis/shared"
    tasks_from: "apt_install.yml"
  vars:
    apt_packages: "{{ munin_node_packages }}"

...
