---

- name: "Copy .bashrc - {{ user }}"
  ansible.builtin.copy:
    src: bashrc
    dest: ~{{ user }}/.bashrc
    mode: "644"

- name: "Copy .vimrc - {{ user }}"
  ansible.builtin.copy:
    src: vimrc
    dest: ~{{ user }}/.vimrc
    mode: "644"

...
