---

- name: "Install unattended upgrades"
  ansible.builtin.import_role:
    name: "nudgis/shared"
    tasks_from: "apt_install.yml"
  vars:
    apt_packages: "{{ unattended_upgrades_packages }}"

...
